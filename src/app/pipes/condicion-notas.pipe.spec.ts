import { CondicionNotasPipe } from './condicion-notas.pipe';

describe('CondicionNotasPipe', () => {
  it('create an instance', () => {
    const pipe = new CondicionNotasPipe();
    expect(pipe).toBeTruthy();
  });
});
